# `@fuseworks/prettier-config`

> FuseWorks Shared [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @fuseworks/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@fuseworks/prettier-config"
}
```
